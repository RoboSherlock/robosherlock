annotator:
  description: Filters the single object hypothesis closest to the that of id OBJ_ID_TRACK
    during the first process call of ClosestHypothesisFilter, taking potential movement
    of that hypothesis into account.
  implementation: rs_ClosestHypothesisFilter
  name: ClosestHypothesisFilter
capabilities:
  inputs:
  - rs.scene.ObjectHypothesis
  outputs:
